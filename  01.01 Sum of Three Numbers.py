firstnumber = input("Enter first number: ")
secondnumber = input("Enter second number: ")
thirdnumber = input("Enter third number: ")
sum = int(firstnumber) + int(secondnumber) + int(thirdnumber) 
print(sum)

#Cant do it this way because they aren't all integers, they are 
#strings right now, we need to convert them all to integers
#We do this by adding "int" in front of the variable to convert it
